// secrets.h
#pragma once
const char* ssid = ""; // Replace with your WiFi SSID
const char* password = ""; // Replace with your WiFi password#include "secrets.h"

// MQTT broker settings
const char* mqtt_server = "";
const int mqtt_port = 1883;
const char* mqtt_user = "";      // Set to "" if not used
const char* mqtt_pass = "";  // Set to "" if not used

